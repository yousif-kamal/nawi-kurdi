import 'package:flutter/material.dart';

class KurdishNamesLst extends StatelessWidget {
  const KurdishNamesLst({Key? key}) : super(key: key);

  //TODO create data model

  //TODO create class for the kurdish names service

  //TODO create a method to get the lis]t of kurdish names

  //TODO render the list of users to the screen

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: [
          Container(
            padding: EdgeInsets.all(20),
            child: Text("ddd"),
          ),
        ],
      ),
    );
  }
}
